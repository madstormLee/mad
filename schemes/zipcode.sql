create table zipcode (
		zipcode char(7),
		serialNo tinyint unsigned,
		sido varchar(10),
		sigungu varchar(20),
		dong varchar(30),
		ri varchar(30),
		doso varchar(20),
		bunji varchar(30),
		apt varchar(50),
		changeDay date,
		juso varchar(100)
		);
